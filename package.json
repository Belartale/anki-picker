{
    "scripts": {
        "write": "npx prettier . --write",
        "dev": "onchange \"**/*\" -- prettier --write --ignore-unknown {{changed}}"
    },
    "type": "module",
    "devDependencies": {
        "onchange": "^7.1.0",
        "prettier": "3.3.2"
    }
}
